Excute these commands to run this server:

Install required package from node
```bash
npm install
```

Run server
```bash
node index.js
```

Note: This is dev environment so in product mode maybe thoose command will change